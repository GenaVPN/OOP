
    
    
    <Window x:Class="OOP_LABA_9_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OOP_LABA_9_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="480" Width="841" Background="#FFE3FFFA"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">

    <Window.Resources>
        
        
        <ResourceDictionary>
            
            <!-- Подключение внешнего словаря -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ModernTextBox" TargetType="TextBox">
                <Setter Property="Padding" Value="8,5"/>
                
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border CornerRadius="5" BorderThickness="1" BorderBrush="{TemplateBinding BorderBrush}" Background="White">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


            <Style x:Key="ActionButton" TargetType="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- другие локальные ресурсы, если нужны -->
        </ResourceDictionary>
        
    </Window.Resources>
        
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*" />
            <ColumnDefinition Width="70*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" CornerRadius="10,10,10,10" Background="White">
            <ScrollViewer  Margin="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,5,5">

                        <TextBlock TextWrapping="Wrap" Text="Панель управления" FontWeight="Bold" Foreground="#444"/>
                        
                    </StackPanel>
                    
                    

                    <Border Margin="0,5,5,5" >
                        <StackPanel>
                            <TextBlock  FontWeight="SemiBold" Foreground="#444" TextWrapping="Wrap" Text="Количество элементов"/>
                            <TextBox x:Name="TextBoxElements" Style="{StaticResource ModernTextBox}" TextWrapping="Wrap"  Margin="0,5,0,0"/>

                        </StackPanel>
                    </Border>

                    
                    


                    <Border>
                        <Button Click="B_click_ADD" Style="{StaticResource ActionButton}" Content="СОЗДАТЬ" Background="#FF33F121" FontWeight="Bold" Foreground="White" Margin="0,9,5,0"/>
                    </Border>
                    <Border>
                        <Button Click="Pokaz" Style="{StaticResource ActionButton}" Content="ПОКАЗАТЬ" Background="#FFDC2B2B" FontWeight="Bold" Foreground="White" Margin="0,9,5,0"/>
                    </Border>

                    <Border Margin="0,5,5,0" >
                        <StackPanel>
                            <TextBlock Foreground="#444" TextWrapping="Wrap" Text="ФИЛЬТР" Margin="0,5,0,0" FontWeight="Bold"/>
                            <TextBox Style="{StaticResource ModernTextBox}" TextWrapping="Wrap" Margin="0,5,0,0" x:Name="TextBoxFilter"  />
                            <Button Click="Filtr_button" Style="{StaticResource ActionButton}"  Content="ПОИСК" Margin="0,5,0,0" Background="#FF4FDD67" FontWeight="Bold" Foreground="White"/>

                        </StackPanel>
                    </Border>
                    
                    
                </StackPanel>
                
                
            </ScrollViewer>
        </Border>

        <Border Padding="10"  Grid.Column="1" Grid.Row="0" CornerRadius="10,10,10,10" Margin="10,0,0,0" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Foreground="#444" Text="VEHICLE LIST" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                <Border Grid.Row="1" BorderBrush="#FFEEEEEE" BorderThickness="2,2,2,2" CornerRadius="4,4,4,4">
                    <DataGrid x:Name="DATAGRID" Grid.Row="1" Background="Transparent" BorderThickness="0" 
          AutoGenerateColumns="False" CanUserAddRows="False"
          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEE"
          HeadersVisibility="Column" RowHeight="50" SelectionMode="Single" IsReadOnly="True">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F8F8"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDD"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ICON" Binding="{Binding ICON}" Width="60"/>
                            <DataGridTextColumn Header="MARK" Binding="{Binding MARK}" Width="100"/>
                            <DataGridTextColumn Header="NUMBER" Binding="{Binding NUMBER}" Width="120"/>
                            <DataGridTextColumn Header="TOTAL CAPACITY" Binding="{Binding MaxWeight}" Width="130"/>
                            <DataGridTextColumn Header="INFO" Binding="{Binding INFO}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Border>
                
            </Grid>

        </Border>
        

        
        
        
    </Grid>
</Window>
