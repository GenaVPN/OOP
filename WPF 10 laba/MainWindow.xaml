<Window x:Class="WPF_10_laba.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>


        </ResourceDictionary>


    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*" />
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FF2C3E50">
            <StackPanel Margin="0,10,0,10">
                <TextBlock  Margin="0,20,0,20" Style="{StaticResource TextStyle}" HorizontalAlignment="Center" Text="Аптека +" Background="{x:Null}" Foreground="{StaticResource ColorText}"  />
                <Button Content="Добавить" Style="{StaticResource TextBox}" Click="AddObj" />
                <Button Content="Удалить" Style="{StaticResource TextBox}" Click="Button_Click"/>
                




            </StackPanel>
        </Border>


        <Grid Margin="10" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Журнал" Style="{StaticResource TextStyle}"/>
            




            <DataGrid Margin="0,10,0,0"  Grid.Row="1" x:Name="Table" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
          HorizontalScrollBarVisibility="Auto" SelectionMode="Single">
                <DataGrid.Columns>


                    <DataGridTextColumn Header="Пациент" Binding="{Binding Patients}" Width="*"/>
                    <DataGridTextColumn Header="Лекарство" Binding="{Binding Name_medicines}" Width="*"/>
                    <DataGridTextColumn Header="Поставщик" Binding="{Binding Provider}" Width="*"/>
                    <DataGridTextColumn Header="Кол-во" Binding="{Binding count}" Width="80"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Cena, StringFormat={}{0} ₽}" Width="100"/>


                </DataGrid.Columns>


            </DataGrid>



        </Grid>
    </Grid>
</Window>

