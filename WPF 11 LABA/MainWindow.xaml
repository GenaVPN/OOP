<Window x:Class="WPF_11_LABA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_11_LABA"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>


        </ResourceDictionary>


    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*" />
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FF2C3E50">
            <StackPanel Margin="0,10,0,10">
                <TextBlock  Margin="0,20,0,20" Style="{StaticResource TextStyle}" HorizontalAlignment="Center" Text="Список городов" Background="{x:Null}" Foreground="{StaticResource ColorText}"  />
                <Button Content="Добавить" Style="{StaticResource TextBox}" Command="{Binding AddCommand}"/>
                <Button Content="Удалить"  Command="{Binding DelCommand}" Style="{StaticResource TextBox}"/>
                <Button Content="Сортировать" Style="{StaticResource TextBox}" Command="{Binding SortCommand}" />

                <TextBlock FontSize="16" Margin="10,20,0,10" Background="{x:Null}" Foreground="{StaticResource ColorText}" Text ="Поиск"/>
                <TextBox Padding="2" Margin="10,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                





            </StackPanel>
        </Border>


        <Grid Margin="10" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Журнал" Style="{StaticResource TextStyle}"/>





            <DataGrid Margin="0,10,0,0" SelectedItem="{Binding SelectedCity}"  ItemsSource="{Binding Cities}" Grid.Row="1" x:Name="Table" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
          HorizontalScrollBarVisibility="Auto" SelectionMode="Single">
                <DataGrid.Columns>


                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="*"/>
                    <DataGridTextColumn Header="Население" Binding="{Binding Population}" Width="*"/>
                    


                </DataGrid.Columns>


            </DataGrid>



        </Grid>


    </Grid>
</Window>
